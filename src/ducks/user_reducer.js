import axios from 'axios';

//const



//action creators

const initialState = {
    user: {},

}

export default function user_reducer(state = initialState, action){
    switch (action.type) {
        default:
        return state;
    }
}